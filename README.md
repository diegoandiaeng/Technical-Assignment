The repository includes:
- Notebook for training the PointNet architecture used for field prediction PointNet training.
- Notebook for training the Scalar Head using the pre-trained PointNet architecture (global encoder).
- Notebook for unsupervised ML techniques using the econded representation.
- Notebook for an interactive UI for testing to load and show deformed component (intended for presentation)
